# Memory - 2026-04-25

## 1. O que foi feito

Triagem automática do item position 103 (id 236) "Construindo APIs testáveis com Node.js" da fonte ebook_foundation.

## 2. Decisões tomadas

- Item rejeitado como `triage_rejected` com reason `no_pdf`
- **Motivo**: O livro existe (GPLv3, autor Waldemar Neto, 196 páginas, PT-BR), mas o PDF compilado só está disponível via fluxo de checkout do Leanpub (mesmo gratuito). O repositório GitHub tem apenas os capítulos em markdown (manuscript), sem release com PDF compilado.
- Não é duplicata — consulta ao banco `sharebook` (`"Books"`) não encontrou match.
- Não é paywall — o livro é genuinamente gratuito no Leanpub.
- Não é pirataria — é GPLv3, conteúdo original do autor.
- Simplesmente não tem PDF acessível para download automatizado.

## 3. Contexto relevante

- DSN do importer: `IMPORTER_DB_DSN` em `sharebook-ebook-importer/.env`
- Há 58 itens em `waiting_triage`, 42 em `waiting_editor`, 20 duplicate, 57 triage_rejected, 111 done.
- A skill `sharebook-triage` define `no_pdf` como uma das razões de rejeição, explicitamente pensada para casos como esse (conteúdo existe mas sem PDF compilado).

## 4. Fricções e soluções

- Leanpub não expõe URL direta de PDF para download automatizado mesmo para livros gratuitos.
- GitHub tem o conteúdo fonte (markdown), mas sem release. Seria possível compilar com pandoc/leanpub-format, mas isso está fora do escopo da triagem.
- Nenhuma fricção técnica no fluxo — tudo rodou suave.

## 5. Como me senti

n/a — execução automática via cron, sem intervenção humana.
