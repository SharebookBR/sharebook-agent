# 2026-04-26 19:07 UTC — Triage Auto (cron)

## O que foi feito
- Batida automática de triagem solicitada por cron.
- Conectei no banco `sharebook_importer` (schema `importer`) via VPS e verifiquei a fila.
- Nenhum item em `waiting_triage`. Fila limpa.

## Estado atual do pipeline
- `done`: 114
- `duplicate`: 22
- `source_blocked`: 5
- `triage_rejected`: 96
- `waiting_editor`: 56
- `waiting_triage`: 0
- `triaging`: 0
- `error`: 0

## Decisões
- Nada a triar nesta batida. Cron encerrado sem ação.

## Contexto relevante
- O importer roda com PostgreSQL (database `sharebook_importer`) no container `fgsgwsckccgk8sccc4gg0gg0` na VPS.
- Produção (catálogo público) fica no database `sharebook`, schema `public`.
- RO user `sharebook_ai_ro` só enxerga `sharebook` (prod), não o importer.
- Para o importer, precisei acessar via SSH no VPS e usar `postgres` user.
