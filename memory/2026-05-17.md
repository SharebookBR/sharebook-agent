# 2026-05-17

- Rodamos preparação editorial BaixeLivros para itens infantis/juvenis, incluindo 609 (Cantigas) e 610 (Os Músicos de Bremen).
- A fila de `waiting_editor` ficou vazia em alguns pontos, então a atividade foi cancelada conforme regra.
- Descobrimos uma fricção no worker de triagem: `No module named 'pypdf'` ao extrair texto de PDF.
- Atualizei o `pyproject.toml` do `sharebook-ebook-importer` para incluir `pypdf`.
- Como o Python do sistema bloqueou `pip install -e .` por ambiente gerenciado, usei um `.venv` local para instalar o projeto.
- Ajustei o `triage_worker.py` para usar `pypdf` quando disponível e cair para `PyMuPDF` (`fitz`) como fallback.
- Atualizei o `BOOTSTRAP.md` do `sharebook-agent` para incluir `pypdf` e uma validação explícita do worker real.
- Verificação do ambiente mostrou: `psql`, `netlify`, `pdftoppm`, `mutool` e `PyMuPDF` disponíveis; `dotnet` ausente no host.
