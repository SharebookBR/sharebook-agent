# Memória Episódica — 2026-05-04

- Sessão focada em taxonomia de `Infantil/Juvenil` no Sharebook.
- Descoberta operacional importante: o preflight do `exec` no OpenClaw bloqueia `python3 ...` quando vem embrulhado em shell complexo (`cd ... && ...`). O formato estável é comando direto com `workdir`, por exemplo `python3 scripts/sharebook_prod_book.py categories`.
- Confirmado que `POST /api/Category` funciona para criar categorias filhas em produção. Foram criadas 8 subcategorias sob `Infantil/Juvenil`:
  - Clássicos Infantis
  - Histórias para Dormir
  - Educativos / Aprendizado
  - Aventuras e Fantasia
  - Animais e Natureza
  - Valores e Emoções
  - Cultura Brasileira / Folclore
  - Diversão e Humor
- Confirmado via Postgres RO que `Type=0` em `Books` corresponde a físico e `Type=1` a digital.
- Contagem inicial em `Infantil/Juvenil`: 221 físicos e 36 digitais.
- Todos os 36 livros digitais foram reassociados da categoria pai para categorias folha.
- Primeira leva de 26 digitais foi classificada por título/sinal editorial e movida via API (`PUT /api/Book/{id}`).
- Segunda leva de 10 digitais foi revisada pela sinopse antes da movimentação e também movida via API.
- Resultado: zero digitais restantes na categoria pai `Infantil/Juvenil`.
- Foi feito teste controlado com subagente `gpt-5.4-mini` para classificar 1 livro físico. O mini escolheu `Diário de um Banana` -> `Diversão e Humor`, com confiança alta. Revisão humana concordou. Sinal positivo para usar mini como triador editorial com supervisão.
